package CardEngine;


public class CardGenerator {
//    CardSet set; // We should be the user of the api, ie, we should saty: GenerateFrom(Set); // walk the set' which's already build
    // and return a player with 8 cards;

    protected CardGenerator() {
    }

    public void Generate() {
        var CardSet = CardEngine.CardSet.Own();



        // Generate from;
    }

}
